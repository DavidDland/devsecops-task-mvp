apiVersion: v1
kind: Service

metadata:
  name: task-mvp-service

spec:
  selector:
    app: task-mvp

  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}

  type: {{ .Values.service.type }}